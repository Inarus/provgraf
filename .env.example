# === Postgres (local, Docker) ===
DATABASE_URL=postgresql://provgraf:provgraf_local_dev@localhost:5632/provgraf

# === CLI ===
LOG_LEVEL=INFO

# === Future (embeddings/Ollama — NOT in the pilot) ===
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_EMBED_MODEL=bge-m3
